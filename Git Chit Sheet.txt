git status
git add [file name]
git add .
git reset [file name] //unstage a file while retaining the changes in working directory
git diff //difference of what is changed but bot staged
git diff --staged // diff of hwat is changed but not yet committed
